# 🚀 Инструкции по развертыванию обновлений на VDS

## 📋 **ЧТО БЫЛО ДОБАВЛЕНО:**

### ✅ **НОВАЯ ФУНКЦИЯ - Редактирование правил и описания турнира:**
- **UI**: Администраторы и создатели могут редактировать правила и описание турнира
- **Layout**: Изменена структура вкладки "Информация" - описание над основной информацией в левом столбце
- **API**: Добавлен PATCH endpoint для обновления турниров с проверкой прав доступа

### ✅ **Основное исправление:**
- **Проблема**: В блоке "Призеры турнира" ничего не отображалось для завершенных турниров
- **Решение**: Добавлена система динамического определения призеров + каждый участник команды теперь призер

### 🎯 **Новая особенность:**
- **Индивидуальные призеры**: Каждый участник команды-победителя получает статус призера
- **Статистика**: Правильный учет всех участников команды в базе данных

### 🔧 **Исправление миграции:**
- **Проблема**: Ошибка foreign key constraint в tournament_logs
- **Решение**: Миграция теперь безопасно проверяет существование турниров

## 🖥️ **КОМАНДЫ ДЛЯ РАЗВЕРТЫВАНИЯ НА VDS:**

### 1. Подключение к серверу:
```bash
ssh root@80.87.200.23
# Пароль: 01012006Fortnite!
```

### 2. Переход в директорию проекта:
```bash
cd /var/www/1337community.com/
```

### 3. Получение обновлений из GitHub:
```bash
git pull origin main
```

### 4. Установка зависимостей (если нужно):
```bash
# Backend зависимости
npm install

# Frontend зависимости
cd frontend
npm install
cd ..
```

### 5. Сборка frontend:
```bash
cd frontend
npm run build
cd ..
```

### 6. Перезапуск backend сервиса:
```bash
# Если используется PM2
pm2 restart 1337-backend

# Или если используется systemctl
systemctl restart 1337-backend
```

### 7. Перезагрузка Nginx:
```bash
systemctl reload nginx
```

## 🔍 **ПРОВЕРКА РАБОТОСПОСОБНОСТИ:**

### 1. Проверка статуса сервисов:
```bash
# Проверка backend
pm2 status
# или
systemctl status 1337-backend

# Проверка Nginx
systemctl status nginx
```

### 2. Проверка логов:
```bash
# Логи backend (PM2)
pm2 logs 1337-backend

# Логи Nginx
tail -f /var/log/nginx/error.log
tail -f /var/log/nginx/access.log
```

### 3. Проверка новой функции редактирования:
```bash
# Откройте любой турнир на сайте как администратор/создатель
# Проверьте наличие кнопок "Редактировать" в блоках описания и правил
# Протестируйте сохранение изменений
```

## ✅ **ПРОВЕРКА ФУНКЦИОНАЛА:**

### Редактирование правил и описания:
1. **Откройте турнир** как администратор или создатель
2. **Проверьте новый layout** вкладки "Информация":
   - Левый столбец: "Описание турнира" (сверху) → "Основная информация" (снизу)
   - Правый столбец: "Правила турнира" с кнопкой редактирования
3. **Нажмите "Редактировать"** для правил или описания
4. **Введите текст** и нажмите "Сохранить"
5. **Проверьте обновления** - изменения должны отображаться сразу

### Система призеров (предыдущий функционал):
1. **Откройте завершенный турнир** на сайте
2. **Проверьте блок "Призеры турнира"** - должен отображаться с подиумом
3. **Для командных турниров** - проверьте, что каждый участник команды показан как призер
4. **Кликните по именам игроков** - должны вести на профили

## 🚨 **В СЛУЧАЕ ПРОБЛЕМ:**

### Если функция редактирования не работает:
```bash
# Проверьте права доступа к файлам
chown -R www-data:www-data /var/www/1337community.com/

# Проверьте логи на наличие ошибок API
pm2 logs 1337-backend --lines 50

# Убедитесь, что поля description и rules существуют в БД
sudo -u postgres psql your_database_name -c "\d+ tournaments"
```

### Если кнопки редактирования не отображаются:
```bash
# Очистите кеш браузера
# Проверьте, что CSS файлы обновились
# Убедитесь, что пользователь имеет права администратора/создателя
```

### Если ошибка сохранения:
```bash
# Проверьте логи backend на ошибки PATCH запроса
pm2 logs 1337-backend | grep "PATCH\|tournaments\|ERROR"

# Проверьте права доступа пользователя в базе данных
```

### Если призеры не отображаются (старая проблема):
```bash
# Проверьте консоль браузера на ошибки JavaScript
# Проверьте логи backend на ошибки API
pm2 logs 1337-backend --lines 50
```

### Если 500 ошибка сервера:
```bash
# Проверьте логи Nginx и backend
tail -f /var/log/nginx/error.log
pm2 logs 1337-backend --lines 20
```

## 📁 **ОСНОВНЫЕ ИЗМЕНЕННЫЕ ФАЙЛЫ:**

### Frontend:
- `frontend/src/components/TournamentDetails.js` - ✅ Добавлена функция редактирования
- `frontend/src/components/TournamentDetails.css` - ✅ Новые стили для редактирования

### Backend:
- `backend/routes/tournaments.js` - ✅ Добавлен PATCH endpoint
- Database: используются существующие поля `description` и `rules`

### Предыдущие изменения:
- `backend/routes/tournaments.js` - ✅ Система призеров (обновлен)
- `database/migrations/add_tournament_winners.sql` - ✅ Исправлен (убрана ошибка foreign key)

## 🎯 **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:**

После развертывания пользователи увидят:

### Новый функционал:
- ✅ **Кнопки "Редактировать"** в блоках описания и правил (только для админов/создателей)
- ✅ **Инлайн редакторы** с автосохранением и валидацией
- ✅ **Обновленный layout** информационной вкладки
- ✅ **Real-time обновления** через WebSocket

### Существующий функционал:
- ✅ **Блок призеров** отображается для всех завершенных турниров
- ✅ **Каждый участник команды** показан как индивидуальный призер с медалью
- ✅ **Ссылки на профили** работают корректно
- ✅ **Красивые анимации** и стили для призеров
- ✅ **Корректная статистика** для всех участников команд

---

📞 **Поддержка**: Если возникли проблемы, проверьте файл `individual_winners_update.md` для детального описания изменений системы призеров. 