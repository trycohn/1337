# КОМАНДЫ ДЛЯ VDS СЕРВЕРА (выполнить по SSH)

# 1. Переходим в директорию проекта
cd /var/www/1337community.com

# 2. Получаем обновления с GitHub
git fetch origin main
git reset --hard origin/main

# 3. Перезапускаем backend сервер
sudo systemctl restart 1337-backend

# 4. Проверяем статус сервера
sudo systemctl status 1337-backend

# 5. Смотрим логи (опционально)
sudo journalctl -u 1337-backend -f

# ИЛИ выполните скрипт одной командой:
bash deploy-rate-limit-fix.sh 